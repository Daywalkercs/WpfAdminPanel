using System.Runtime.CompilerServices;
using System.Windows;
using WpfAdminPanel.Views;

namespace WpfAdminPanel
{
    public partial class App : Application
    {
        //protected override void OnStartup(StartupEventArgs e)
        //{
        //    base.OnStartup(e);

        //    // Создаем и показываем окно входа
        //    var loginWindow = new LoginPasswordWindow();
        //    bool? result = loginWindow.ShowDialog();

        //    // Если вход успешен — показываем главное окно
        //    if (result == true)
        //    {
        //        var mainWindow = new MainWindow();
        //        mainWindow.Show();
        //    }
        //    else
        //    {
        //        // Если вход не выполнен — закрываем приложение
        //        Shutdown();
        //    }
        //}
    }


}
